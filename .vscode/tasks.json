{
  "tasks": [
    {
      "type": "shell",
      "label": "build",
      "command": "sh",
      "args": ["-c", "meson setup debug && meson compile -C debug"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": ["$gcc"],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "detail": "Build meson project in debug mode"
    }
  ],
  "version": "2.0.0"
}
